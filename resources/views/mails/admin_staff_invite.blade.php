<h1>INVITE STAFF ADMIN REGISTRY ACCOUNT</h1>
<div>
    <span>Token: </span>
    <a href="{{$app_url}}/admin-staff-registry/{{$token}}">{{$token}}</a>
</div>
<div>
    <b>Link: </b>
    <span>{{$app_url}}/admin-staff-registry/{{$token}}</span>
</div>